<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="free.ytalerts.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="free.ytalerts.app.gui.app.SkyTubeApp"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="free.ytalerts.app.gui.activities.MainActivity"
            android:theme="@style/NoActionBarActivityTheme">
        </activity>
        <activity
            android:name="free.ytalerts.app.gui.activities.YouTubePlayerActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/VideoPlayerActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="youtube.com" />
                <data android:host="m.youtube.com" />
                <data android:host="www.youtube.com" />
                <data android:host="youtu.be" />
            </intent-filter>
        </activity>
        <activity android:name="free.ytalerts.app.gui.activities.PreferencesActivity" />
        <activity android:name="free.ytalerts.app.gui.activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="free.ytalerts.app.gui.activities.DoubtsActivity" />
        <activity android:name="free.ytalerts.app.gui.activities.AboutActivity"/>
    </application>

</manifest>